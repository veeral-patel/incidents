<%= form_with(model: ticket_template, local: true) do |form| %>
  <% if ticket_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_template.errors.count, "error") %> prohibited this ticket_template from being saved:</h2>

      <ul>
      <% ticket_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
