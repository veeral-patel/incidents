<p id="notice"><%= notice %></p>

<h1><%= @ticket.name %> > Attachments</h1>

<a href=".">Details</a>
<a href="/tickets/<%= @ticket.id =%>/attachments">Attachments (<%= @ticket.attachments.count =%>)</a>

<br /> <br />

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <td>
    <% @ticket.attachments.each do |attachment| %>
      <tr>
        <td><%= attachment.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Attachment', new_ticket_attachment_path %>
