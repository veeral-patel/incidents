<%= form_with(model: observable, local: true) do |form| %>
  <% if observable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(observable.errors.count, "error") %> prohibited this observable from being saved:</h2>

      <ul>
      <% observable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :observable %>
    <%= form.text_field :observable %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :ticket_id %>
    <%= form.text_field :ticket_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
